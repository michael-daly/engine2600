const CANVAS_WIDTH  = 160;
const CANVAS_HEIGHT = 192;

// Although PAL and especially SECAM had fewer individual colors than NTSC, they still used the same
// color/luminance grade.
const NUM_COLORS = 128;


export
{
	CANVAS_WIDTH,
	CANVAS_HEIGHT,

	NUM_COLORS,
};
